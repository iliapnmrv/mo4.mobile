import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const HorizontalListSeparator = () => {
  return (
    <View
      style={{
        height: 50,
        width: 10,
      }}
    />
  );
};

export default HorizontalListSeparator;

const styles = StyleSheet.create({});
